# Exact package versions used to produce the current TSP paper results.
#
# This is NOT auto-generated by `pip freeze` (that would capture hundreds of
# unrelated packages from the machine's global Python install). It is a
# hand-picked list of only the packages this project actually imports,
# pinned to the exact versions verified against results/tuning/ and the
# canonical results/*_comparison folders.
#
# Install with:
#   pip install -r requirements-lock.txt
#   pip install -e . --no-deps
#
# Captured: 2026-08-07, Python 3.13.5, Windows.
# See TEAM_SETUP.md for full instructions.

numpy==2.3.5
pydantic==2.11.9
pydantic-settings==2.12.0
typer==0.19.2
rich==14.1.0

# web extra
fastapi==0.141.1
uvicorn==0.48.0

# ml extra (feature selection — version-sensitive: KNN/CV tie-breaking
# can differ across sklearn versions, see audit_checklist.md P2-05)
scikit-learn==1.7.2
pandas==2.3.3

# viz extra
matplotlib==3.10.6

# dev extra
pytest==9.1.1
ruff==0.16.1
httpx==0.28.1
